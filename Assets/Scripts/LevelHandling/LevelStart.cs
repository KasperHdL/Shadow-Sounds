using UnityEngine;
using System.Collections;

public class LevelStart : MonoBehaviour {
    
    [TextArea] public string explaination = "Place this object where the player is supposed to start in the level";

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
